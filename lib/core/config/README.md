# MediQueue Config Directory

This directory contains configuration files and documentation for the MediQueue app.

- `app_config.dart`: Centralized app configuration values (API base URL, database name, refresh intervals, etc.)

## Adding More Configs
Add additional config files or constants as needed for environment, feature flags, or secrets.
